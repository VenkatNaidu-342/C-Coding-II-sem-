#include<stdio.h>

int main() {
	int arr[] = {10, 20, 10, 20, 30, 20, 40};
	int size = sizeof(arr) / sizeof(arr[0]);
	int ele, freq = 0, i;
	
	scanf("%d", &ele);
	
	for(i = 0; i < size; i++) {
		if(arr[i] == ele) {
			freq++;
		}
	}
	
	printf("\n%d times.\n", freq);
	
	return 0;
}
